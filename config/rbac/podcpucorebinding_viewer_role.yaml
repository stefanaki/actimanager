# permissions for end users to view podcpucorebindings.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: podcpucorebinding-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: actimanager
    app.kubernetes.io/part-of: actimanager
    app.kubernetes.io/managed-by: kustomize
  name: podcpucorebinding-viewer-role
rules:
- apiGroups:
  - cslab.ece.ntua.gr
  resources:
  - podcpucorebindings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - cslab.ece.ntua.gr
  resources:
  - podcpucorebindings/status
  verbs:
  - get
